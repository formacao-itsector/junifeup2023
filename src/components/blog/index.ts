import { Section, HorizontalDivider, Link } from './blog.components';
import { Text, Title, Subtitle } from './blog.typography';

export { Blog, BlogPostProps } from './blog';

export const BlogComponents = {
  Section,
  HorizontalDivider,
  Link,
};

export const BlogTypography = {
  Text,
  Title,
  Subtitle,
};
